# Transaction Fraud Detection
### ENGE707 - ML Project

## Setting up the project
We'll be using python 3.10.x, so use any variation of that.

    1. `python -m venv .venv`
    2. Select kernel within notebook
    3. Run step 1 of the notebook to install deps